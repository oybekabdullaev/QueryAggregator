@model QueryAggregator.Core.Domain.Query

@{
    ViewBag.Title = "Home Page";
}

<h1>Results for: @Model.QueryString</h1>

@foreach (var link in Model.Links)
{
<article>
    <h3>
        <a href="@link.Url">@link.Title</a>
    </h3>
    <p>
        @link.Description
    </p>
    <hr/>
</article>
}